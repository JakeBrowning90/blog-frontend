const express = require('express');
const router = express.Router();

// Create comment (YES)

// Read ALL comments

// Read comment

// Update comment (YES)

// Delete comment (YES)

module.exports = router;