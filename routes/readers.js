const express = require('express');
const router = express.Router();

// Reader Login (YES)

// Create reader (YES)

// Read ALL reader

// Read reader

// Update reader

// Delete reader

module.exports = router;